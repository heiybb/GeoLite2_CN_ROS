{
  "name": "cidr-lists-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "compare": "node test/compare.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
